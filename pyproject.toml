[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "email-automation-jev"
version = "0.2.0"
description = "Gmail ingestion and Jev-powered job email classification"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12,<5",
  "google-api-python-client>=2.160,<3",
  "google-auth-oauthlib>=1.2,<2",
  "python-dotenv>=1.0,<2",
  "supabase>=2.15,<3",
  "typesafe-sdk>=0.7.1,<0.8",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.15,<2",
  "pytest>=8.3,<9",
  "ruff>=0.11,<1",
]

[project.scripts]
email-automation = "email_automation_jev.commands.dispatch:main"
email-dashboard = "email_automation_jev.dashboard:main"
email-gmail-auth = "email_automation_jev.commands.gmail:authorize"
email-ingest = "email_automation_jev.commands.gmail:ingest"
email-inspect = "email_automation_jev.commands.gmail:inspect"
email-sample = "email_automation_jev.commands.labeling:sample"
email-jev-prepare = "email_automation_jev.commands.evaluation:prepare"
email-jev-evaluate = "email_automation_jev.commands.evaluation:evaluate"
email-phase5-bootstrap = "email_automation_jev.commands.storage:bootstrap"
email-phase6-classify = "email_automation_jev.commands.classify:run"

[tool.hatch.build.targets.wheel]
packages = ["src/email_automation_jev"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.mypy]
python_version = "3.11"
check_untyped_defs = true
ignore_missing_imports = true
packages = ["email_automation_jev"]

[[tool.mypy.overrides]]
module = [
  "email_automation_jev.repository",
  "email_automation_jev.classification_repository",
  "email_automation_jev.labeling_store",
  "email_automation_jev.dashboard",
  "email_automation_jev.cli",
  "email_automation_jev.classification_worker",
]
ignore_errors = true

[tool.ruff]
line-length = 160
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
